Flask>=3.0,<4
Flask-SQLAlchemy>=3.1,<4
celery>=5.3,<6
redis>=5,<7
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
Pillow>=10
gunicorn>=22
Authlib>=1.3
requests>=2.31
huggingface_hub>=0.24
datasets>=2.20,<3.0
# h5py: required by some HF dataset loading scripts (notably
# sayakpaul/nyu_depth_v2 which reads .h5 source files at stream
# time). Other HDF5-backed depth / medical / scientific datasets
# need it too. Pre-installing here is far cheaper than pip-installing
# at task time inside an HF stream that already holds the volume lock.
h5py>=3.10
# soundfile: HF Audio feature decoding routes through it (the datasets
# lib raises "please install 'soundfile'" otherwise). Required for any
# PWC audio benchmark — speech recognition, keyword spotting, etc.
soundfile>=0.12
